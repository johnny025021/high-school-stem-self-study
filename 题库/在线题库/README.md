# 在线题库

此目录管理 GitHub Pages 在线题库的路径规则。

## 已上线兼容区

为保证现有链接、目录清单和浏览器缓存继续有效，已经发布的题库仍保留在以下原路径，不移动、不重打包、不改哈希：

- `题库/数学`
- `题库/物理`
- `题库/化学`

详细映射见 `online-path-map.json`。

## 后续规则

只有适合在线交付的轻量题库才进入在线目录和 `data/catalog.json`，并使用 `delivery_mode: online`。含大量高清原书页的大体积书籍默认采用线下题库方式。
