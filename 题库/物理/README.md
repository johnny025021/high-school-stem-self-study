# 物理题库

此目录仅接收 `subject_id: physics` 的题库包。

当前已在 `章节练习/一课一练/2026上册/` 放置第1—10章共10个 Schema 1.1 题库包，合计790题、58节。它们来自已完成章节人工复核的物理发布候选集合；本次仅复制既有 ZIP，不改写包内数据。

网页通过根目录 `data/catalog.json` 按章发现和下载题库。物理页面使用“题库大类 → 书籍 → 章节 → 节／课次 → 题目”层级，当前本地候选须经人工界面确认后再发布。
